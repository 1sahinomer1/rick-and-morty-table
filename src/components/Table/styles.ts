import styled from "styled-components";
import { colors } from "../../theme";

export const Table = styled.table`
  border-radius: 20px;
  border: 1px solid ${colors.neutralColor[500]};
`;
export const StyledTh = styled.th`
  padding: 16px 24px;
`;
